import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const main=fs.readFileSync(new URL('../src/main.jsx',import.meta.url),'utf8');const db=fs.readFileSync(new URL('../src/database.js',import.meta.url),'utf8');
test('release metadata is v1.4.16.37',()=>{assert.match(main,/const VERSION='1\.4\.16\.37'/);assert.match(main,/const BUILD_ID='141637'/);assert.match(db,/version:126,name:'Dynamic Podcast Playlist Registry'/)});
test('registry migration contains all existing playlists and stable capabilities',()=>{for(const id of ['library','up-next','stories','drama','general-interest'])assert.match(db,new RegExp(id.replace('-','\-')));for(const column of ['playlist_type','icon_tone','enabled','is_system','can_rename','can_delete'])assert.match(db,new RegExp(column))});
test('carousel and assignment settings are registry-driven',()=>{assert.match(main,/playlistRegistry=useMemo/);assert.match(main,/playlistRegistry\.map\(row=>/);assert.match(main,/standardPlaylists\.map\(playlist=>/);assert.doesNotMatch(main,/checked=\{playlistSubscriptions\.generalInterest\}/)});
test('custom playlists can be created and renamed while My Podcasts remains protected',()=>{assert.match(main,/const createPlaylist=async/);assert.match(main,/const renameCurrentPlaylist=async/);assert.match(main,/Number\(currentPlaylist\?\.can_rename\)===1/);assert.match(db,/VALUES \('library','My Podcasts'.*0,0\)/s)});
test('playlist behavior uses stable playlist ids',()=>{assert.match(main,/tabPlaylistId=landingTab==='queue'\?'up-next':landingTab/);assert.match(main,/playlistItemsById\[tabPlaylistId\]/);assert.match(main,/savePlaylistSubscription\(playlist\.playlist_id/)});
