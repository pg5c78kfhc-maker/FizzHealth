# Fizz Health v0.1.4

Local-first React/Vite PWA using SQLite in the browser. Personal workbook and populated SQLite files are not included in deployment artifacts.

Build:

```bash
npm ci
npm run build
```
